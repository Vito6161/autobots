#include "Motores.h"
#include "SensoresRefletancia.h"

#define velFrente 80
#define velTras -60

void Motores::Frente()
{
  if(bbbb() || bppb() || pppp() || pbpb() || bpbp() || pbbp()) // frente
  {
    robo.acionarMotores(velFrente, velFrente);
  }

}


void Motores::Direita()
{  
  else if(bppp() || bbpp() || bbbp() || bbpb()) // direita
  {
    
  }
}

void Motores::Esquerda()
{
  else if (pppb() || ppbb() || pbbb() || bpbb()) // esquerda
  {
    
  }
}

void Motores::Re
{
  else if (pbbp())
  {
    
  }
}

  
}
