#ifndef REFLETANCIA_H
#define REFLETANCIA_H

#include <robo_hardware.h>

class Refletancia
{
  public:
    bool Frente();
    bool Esquerda();
    bool Direita();
    bool Parar();
    bool Re();
};

#endif
